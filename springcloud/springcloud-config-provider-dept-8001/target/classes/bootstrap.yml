#系统级别的配置
spring:
  cloud:
    config:
      name: config-provider-dept-8001  #需要从git上读取的资源名称，不需要后缀
      profile: dev
      label: master  #git master分支
      uri: http://localhost:3344   #连接config-server